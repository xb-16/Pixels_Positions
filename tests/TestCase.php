<?php

namespace Tests;

use Illuminate\Foundation\Testing\TestCase as BaseTestCase;

abstract class TestCase extends BaseTestCase{

    // Optional: If you want to run default seeders before tests
    // protected bool $seed = true;

    // Optional: If you want to specify a specific seeder to run
    // protected string $seeder = SpecificDatabaseSeeder::class;
}